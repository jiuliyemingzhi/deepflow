services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: ghcr.io/deepflowio/rust-build:latest
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

